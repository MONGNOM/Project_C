using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Slim : Monster
{
    // Start is called before the first frame update
    void Start()
    {
        base.Start();
       /* detectEnemyRange = new Vector2(3f, 3f);
        attackEnemyRange = new Vector2(1.5f, 1.5f);*/
    }

  
}
